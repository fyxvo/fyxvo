{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @fyxvo/config build && pnpm --filter @fyxvo/database build && pnpm --filter @fyxvo/api build",
    "watchPatterns": [
      "/apps/api/**",
      "/packages/config/**",
      "/packages/database/**",
      "/package.json",
      "/pnpm-lock.yaml",
      "/pnpm-workspace.yaml",
      "/tsconfig.base.json"
    ]
  },
  "deploy": {
    "preDeployCommand": "pnpm --filter @fyxvo/database migrate:deploy",
    "startCommand": "pnpm --filter @fyxvo/api start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1,
    "sleepApplication": false
  }
}
